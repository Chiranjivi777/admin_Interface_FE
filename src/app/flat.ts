export interface Flat {
  id: number;
  ownerName: string;
  email: string;
  flatNumber: number;
  phone: string;
  imageUrl: string;
  flatCode: string;
  lastPaid: Date;
  due: number;

  // name: string;
  // email: string;
  // jobTitle: string;
  // phone: string;
  // imageUrl: string;
  // employeeCode: string;
  // lastPaid: Date;
  // due: number;
}
